spring:
  application:
    name: spring-ai-webflux-stage01
  ai:
    openai:
      # 推荐通过环境变量注入，勿将真实密钥写入仓库
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          # 可按账号可用模型调整；名称需与服务端一致
          model: gpt-4o-mini

server:
  port: 8080
